# For installing the smaller, CPU-only version of PyTorch
--extra-index-url https://download.pytorch.org/whl/cpu

# Core application and server
fastapi==0.116.1
uvicorn[standard]==0.30.1
pydantic==2.11.7
pydantic_settings==2.10.1

# Database
pymongo==4.14.0
dnspython==2.6.1 # Required for MongoDB Atlas +srv connection

# AI & Data Processing
numpy==2.3.2 # Make sure your Docker container uses Python 3.11+ for this
pandas==2.3.1
scikit-learn==1.5.1 # Correct package name
sentence-transformers==3.0.1
spacy==3.7.5
torch

# Utilities
pdfminer.six==20240708 # Only the modern version is needed
requests==2.32.4
python-dotenv==1.0.1 # For loading .env files locally